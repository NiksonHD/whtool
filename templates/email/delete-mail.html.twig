{% extends 'base.html.twig' %}

{% block body %}
    <div class="body-content">

        <div> Избери email</div>
        <hr>

        <form class="form-horizontal" action="{{ path('email-delete') }}" method="POST">


            {% if evaluation.category.name is defined %}
                {{evaluation.category.name}}
            {% endif %}

            {% for email in emails %}
            <lable>
                        {{ email.address }}
                    <input id="search_input"  type="checkbox" name="id" value="{{email.id  }}"/></td>
                        </lable>

                <hr>
                {% endfor %}

            <button type="submit" class="btn btn-primary btn-xs">Изтрий</button>
        </form>
        <div class="container body-container">

        </div>



    </div>


    {{  form_row(form._token) }}  


{% endblock %}
